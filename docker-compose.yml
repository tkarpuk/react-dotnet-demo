version: "3.9"

services:
  backend:
    build:
      context: ./backend/Api/Api
    container_name: books-api
    ports:
      - "5000:8080"

  frontend:
    build:
      context: ./frontend/react-app
    container_name: books-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend